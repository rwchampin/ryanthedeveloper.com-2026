import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";


const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Ryan The Developer | Full Stack React.js, Next.js, Node.js, Three.js Web Developer in Jersey City NJ",
  description: "Full stack web developer specializing in React.js, Next.js, Node.js, Three.js animations, API development, Django, Python, GSAP animations, and responsive web design. Based in Jersey City, NJ, building high-performance, modern, visually stunning websites and applications.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        {children}
      </body>
    </html>
  );
}
